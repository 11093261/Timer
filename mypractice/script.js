const header = document.querySelector("h1")
const say = (blue)=>{
    
}
header.addEventListener("load", say)
header



const player = (event) =>{
    const Event = Date;
    const model = Event.this;
    try{
        model = eval(model)
    }
    catch(e){
        ("error");
    }   

}
